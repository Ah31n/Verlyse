# Current workspace sync

This marker identifies the sanitized application source synced from the workspace export uploaded on 2026-09-04. It contains no credentials or deployment secrets.

The outer workspace archive and legacy deployment artifacts remain intentionally excluded.
